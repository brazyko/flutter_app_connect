import 'package:flutter/material.dart';

class StorageUsagePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Storage Usage')),
      body: Center(child: Text('Storage Usage Content')),
    );
  }
}
